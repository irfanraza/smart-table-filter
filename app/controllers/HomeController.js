/**
 * @name HomeController
 * @description
 * Home controller is the parent of all other components on page.
 *
 */
(function(angular) {

    var app = angular.module('smartTableApp.controllers', []);
    app.controller('HomeController', ['$scope', function($scope) {
        // Any custom logic for home controller goes here
    }]);

})(angular);